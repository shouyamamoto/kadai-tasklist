<%= form_with(model: task , local: true) do |f| %>
  <%= render 'layouts/error_tasks', task: @task %>
  
  <%= f.label :status, 'ステータス' %>
  <%= f.text_field :status %>

  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '送信' %>
<% end %>